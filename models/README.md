# Model assets

Store only manifests, configuration, adapters, and prompts in Git. Record model source, checksum, license, hardware requirements, and runtime compatibility in manifests; do not commit model weights.
